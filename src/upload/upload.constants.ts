export const UPLOAD_FOLDER_MAP: Record<string, string> = {
  avatar: 'avatars',
  'blog-image': 'blog-images',
  'og-image': 'og-images',
};

export const VALID_UPLOAD_TYPES = Object.keys(UPLOAD_FOLDER_MAP);
