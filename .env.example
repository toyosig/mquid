# Database
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=
DB_NAME=
DATABASE_URL=

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=

# App
PORT=
NODE_ENV=
FRONTEND_ORIGIN=

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Redis (optional — omit for in-memory cache in local dev)
# Format: redis://[:password@]host[:port][/db]
REDIS_URL=

# SendGrid (email delivery)
# Get your API key at https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=
# Must be a verified sender in SendGrid (Single Sender Verification — any email works)
SENDGRID_FROM_EMAIL=
